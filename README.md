# Vahid's Blog

A single-page static intro for Vahid's blog, built with plain HTML, CSS, and JavaScript.

It is also installable as a small PWA with a web app manifest, SVG icons, and offline app-shell caching through a service worker.

## Local Preview

```bash
npm start
```

Railway will run the same `npm start` command defined in `railway.toml`.
